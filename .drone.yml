---
kind: pipeline
type: kubernetes
name: test-pipeline

steps:

  - name: prepare
    image: node:lts-alpine
    commands:
      - npm install
      - pwd
      - ls

  - name: test
    image: node:lts-alpine
    commands:
      - npm run lint
      - npm run test

...